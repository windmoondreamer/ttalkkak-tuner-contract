# Windows 버전

`../windows-dist/TtalkkakTuner.Windows.exe`는 Windows 10/11 64비트에서 별도 설치 없이 실행한다.

- 감도와 FPS·MOBA·DESK·MEDIA 키 배치를 저장한다.
- `COM` 포트의 `btnev=XXXX` 진단 출력을 읽어 실시간 입력을 보여 준다.
- Switch 게임 모드 전환 방법을 안내한다.

Windows SmartScreen 경고는 서명되지 않은 개인 배포 프로그램이어서 발생할 수 있다. 소스는 이 폴더의 `Program.cs`에 있다.
